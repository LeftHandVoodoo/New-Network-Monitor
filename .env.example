# Application (UI)
NODE_ENV=development
PORT=5173
LOG_LEVEL=debug

# Local agent
IPC_TOKEN=change-me
PING_TARGET=8.8.8.8
RETRY_COUNT=3
RETRY_INTERVAL_MS=5000

# UI -> agent IPC
VITE_AGENT_PORT=8787
VITE_IPC_TOKEN=change-me

# Network adapter reset requires elevation
# RESET_COMMAND=powershell -ExecutionPolicy Bypass -File .\scripts\reset-adapter.ps1
